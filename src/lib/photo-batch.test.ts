import{describe,expect,it}from'vitest';import{uploadIndividually}from'./photo-batch';
describe('independent photo uploads',()=>{it('keeps completed files out of the retry list while continuing after a failure',async()=>{const completed:string[]=[],calls:string[]=[],files=['one.jpg','two.jpg','three.jpg'];const first=await uploadIndividually(files,async file=>{calls.push(file);if(file==='two.jpg')throw Error('network down');completed.push(file)});expect(completed).toEqual(['one.jpg','three.jpg']);expect(first.failed).toEqual(['two.jpg']);expect(first.errors).toEqual(['network down']);const retry=await uploadIndividually(first.failed,async file=>{calls.push(file);completed.push(file)});expect(retry.failed).toEqual([]);expect(calls).toEqual(['one.jpg','two.jpg','three.jpg','two.jpg']);expect(completed.sort()).toEqual(files.sort());});});
