You are working in my repository. Build my personal storage inventory web app, starting now with a working implementation, not just a proposal. Read AGENTS.md and inspect existing files first. Preserve unrelated work and adapt to useful existing code. Read the accompanying Storage-System-Plan.md if supplied; copy it into docs/storage-system-plan.md. The requirements below make this prompt usable by itself.
Product and stack
Use React, TypeScript and Vite for a mobile-first web app deployed on GitHub Pages. Use Supabase Postgres, Auth and private Storage for live data and photos. Keep frontend, SQL migrations, tests, deployment configuration and documentation in this one repository. No custom always-running backend. Use current stable compatible dependencies and a lockfile. Use browser-safe Supabase publishable credentials only.
This is a private inventory for only me. Create no public registration UI. Document creating my account and disabling new-user signup in Supabase. Default to email/password login. Enforce owner-only database and photo access in backend policies, not merely by hiding UI. The static app shell can be public; inventory and photos must be private.
Internet is required. Do not build offline sync, AI/image recognition, paid AI APIs, QR codes, bag-label printing, multi-user collaboration, visual shelves or automated organisation.
Physical organisation
I create, rename and manage my own free-form storage locations in the UI, e.g. “Basement shelf”, “Desk drawer”. Do not require room/shelf/position levels. Optional nesting of locations is useful, with cycle prevention.
Create boxes within locations. All boxes use one ascending number series across my inventory, starting at 1. Numbers are server-assigned, positive, unique, immutable and never automatically reused. Moving a box keeps its number. Use transactional counter allocation that is safe under concurrent requests. Optional description and capacity in litres.
Items normally belong to a box, but may also belong directly to a location. Require exactly one destination. Display addresses such as “Basement shelf · Box 12”. Derive an item's address from its box so moving a box updates the displayed address of all contents without rewriting each item.
Items and photos
Only name and destination are required. Optional notes, tags, quantity, quantity unit, approximate-quantity flag, estimated total volume in litres and any number of photos. Entries may represent a single tool, a bag of interchangeable parts or a useful set. No physical item/bag ID requirement; use hidden UUIDs for stable data relationships. Unknown quantity is null, not zero; zero is known out-of-stock. Quantity and volume cannot be negative.
Volume describes the whole entry/bag, not one piece. Sum active known estimates per box and show missing-estimate counts. Do not multiply volume by quantity or claim geometric fit. Capacity is optional. Quantity changes should not silently rescale volume.
Support camera capture, repeated detail photos and multi-file gallery selection. Offer ordering/cover choice and photo removal. Compress/re-encode with orientation correction, roughly 2048px longest edge by default, preserving readable details and removing location metadata. Validate actual supported formats and show a useful error for unsupported ones such as HEIC if decoding is unavailable. Use private storage paths and authenticated image access or temporary signed URLs; persist object paths, not signed URLs.
Handle partial failures: item metadata can save while individual photos fail, with clear status and retry. Do not duplicate the item or completed uploads. Reconcile or clean up uploaded objects whose metadata failed to register. Document cleanup for unresolved interrupted operations. Do not claim success until the relevant operations complete. Preserve form values on failure and warn before discarding an unsaved form.
Screens and workflows
- Sign in/sign out and recovery handling.
- Inventory/search: thumbnails, readable destinations, tags/quantity, server-side paginated metadata search over names, notes, tags, box numbers/descriptions and locations. Filters for location, box, tag, archived and out-of-stock. Do not restrict search to the first fetched page.
- Add/edit: name, destination, photos, optional fields collapsed initially; Save and add another keeps the destination and clears other fields.
- Item detail: photo gallery, metadata, quantity adjustment, edit, move, archive and restore.
- Storage: create/edit/nest locations; create/edit/move boxes; box detail with contents and quick add.
- Multi-select item movement. Use an atomic transaction for each batch.
- Settings/export.
Use a clean, practical English interface supporting German names and notes. Make it comfortable on phone and desktop, with accessible controls and large touch targets. Avoid decorative dashboards or onboarding that slows cataloguing. Use reversible archive for entries and retain their photos. Block deleting referenced locations/boxes. One item has one destination; split stock across locations by separate entries initially.
Backend correctness
Create reproducible migrations for locations, boxes, items, item_photos and protected box-number counters. Include owners, UUIDs, timestamps, appropriate indexes, constraints and foreign keys. Enforce same-owner parent references and exactly one item destination. Protect all tables with RLS and Storage with policies validating owner/item paths. Test unauthenticated and second-user denial, including direct API operations. Review any SECURITY DEFINER function carefully, restrict grants/search_path and enforce ownership; prefer invoker security.
Add version-based stale-edit protection to editable records so phone and desktop do not silently overwrite each other's changes. Provide clear conflict feedback. Keep secrets, real inventory exports and photos out of Git. Clear private query/image caches on logout. Do not service-worker-cache private data.
Codex exports and backups
I want to let Codex examine my inventory later; no AI integration in this app. Add versioned JSON export preserving stable IDs and relations, a flattened human-readable CSV, and a schema README. Include archived data and paginate through all rows. Offer a full ZIP export including stored photo files and relative manifest paths, not expiring URLs. Expose download failures and clearly identify an incomplete backup. Control memory use and show progress.
Document how I can give the export to Codex; photo inspection requires the photo bundle. Suggested changes will initially be applied manually in the UI.
Include a documented authenticated restore procedure or local utility for restoring the full archive into an empty inventory. Validate schema, references and photos, remap ownership to the current user, preserve IDs/box numbers where appropriate, advance the number counter and refuse to overwrite populated data. A browser must never receive a privileged key. Verify a backup/restore round trip in a disposable environment. Interactive merge and automatic scheduled backup can wait.
Repository and delivery
Organise src/, supabase/migrations/, relevant tests/, docs/, scripts/ if needed, and .github/workflows/. Include README.md, .env.example, .gitignore, a dependency lockfile and commands for dev, typecheck, lint, tests and build. Keep data access typed and centralised without introducing a large framework around this small app.
Configure GitHub Actions checks and Pages deployment with the correct Vite repository base. Use hash routes so app links/refreshes work on Pages. Handle auth redirects/recovery consistently with the actual base URL. Document applying database migrations separately from frontend deployment. Use official Supabase and Vite docs when checking current APIs. Do not silently deploy or change my remote project merely because you can; preparing the repository is the task.
README must give the exact remaining setup steps: Supabase project, migrations, single account, disable signup, private bucket/policies, environment values, local run, Pages settings/build configuration and phone smoke test. Distinguish browser-safe values from secrets. If services or credentials are missing, finish all possible code/configuration/tests, show a configuration-required state, and tell me precisely what remains. Do not fabricate successful live tests or block all implementation on credentials.
Work in this order: schema/auth/security; locations and numbered boxes; fast item entry and multiple photos; search/detail/moves/archive; export/restore and failure handling; deployment/readme and verification. Deliver the complete usable core and release reliability work. Defer swipe decluttering, detailed movement history, stock-splitting UI and direct Codex integration. Record these in a short backlog.
Run focused checks for concurrent box numbering; same-owner/destination constraints; location cycles; RLS and private photos; failed upload retry; stale updates; address changes after box moves; quantity/volume semantics; search beyond the first page; archive/restore; export completeness and restore; and Pages subpath navigation. Test the main mobile-width workflow with browser tooling if available. Use meaningful tests, not assertions that just mirror implementation. Report which checks actually ran and which need configured infrastructure.
Primary acceptance scenario: I create “Basement shelf”, create Box 1 and Box 2, add “Ikea shelf accessories” with two photos, find it from another device, move it to Box 2, then move Box 2 to another location and see its contents follow. I can also save a name-and-destination-only item and download a complete usable backup.
Make reasonable implementation decisions without repeatedly asking me about routine details. At the end, summarise what works, the validation results and any concrete setup actions still required from me.